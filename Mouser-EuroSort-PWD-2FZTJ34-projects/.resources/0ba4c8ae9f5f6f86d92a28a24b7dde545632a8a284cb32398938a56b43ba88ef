"""
	Incoming WCS request handler

"""
from shared.tools.logging import Logger; Logger().trace('Compiling module')

from wcs.api.base import LoggedEndpoint

class WCS_Incoming(LoggedEndpoint):
	WEBDEV_ENDPOINT = 'wcs/v1'

